import numpy as np
import matplotlib.pyplot as plt

bit = 10100101010
split_bit = [int(i) for i in str(bit)]

width = np.arange(len(split_bit))


plt.x
plt.ylim(-3, 3)

plt.step(width, split_bit)
plt.show()		

